mixin script(filename)
    -var isProduction =  process.env.NODE_ENV === 'production'
    script(type="module", src=(isProduction ? `@/${filename}` : `${filename}`))

mixin head(title, description, keywords, description_social, image_social, url_social, type_social)
  title= title
  meta(name="description", content= description)
  meta(name="keywords", content= keywords)
  meta(property="og:title" content= title)
  if typeof(description_social) !== 'undefined' && description_social != ""
    meta(property="og:description" content= description_social)
  if typeof(image_social) !== 'undefined' && image_social != ""
    meta(property="og:image" content= image_social)
  if typeof(url_social) !== 'undefined' && url_social != ""
    meta(property="og:url" content= url_social)
  if typeof(type_social) !== 'undefined' && type_social != ""
    meta(property="og:type" content= type_social)
  else
    meta(property="og:type" content="website")

mixin menu(link, title, current)
  if current != ""
    li(class= current)
      a(href= link, title= title) !{title}
  else
    li
      a(href= link, title= title) !{title}

mixin menu-link(link, title, current)
  if current != ""
    li
      a(href= link, title= title, class= current) !{title}
  else
    li
      a(href= link, title= title) !{title}

mixin social(link, title, icon)
  li
    a(href= link, title= title)
      i.fa(class= icon)

mixin social-text(link, title, icon)
  li
    a(href= link, title= title)
      i.fa(class= icon)
      | #{title}

mixin reason-item(src, title, summary)
  .col-xs-12.col-sm-4
    .reason-item(data-mh="know")
      figure.reason-media
        img(src=src, width="271", height="180", alt=title)
      h5.reason-title= title
      p.reason-summary !{summary}

mixin course-item(src, title, summary)
  .course-item(data-mh="course")
    figure.course-media
      img(src= src, width="98", height="98", alt= title)
    h5.course-title #{title}
    p.course-summary !{summary}

mixin credibility-item(src, summary)
  .col-xs-12.col-sm-4
    .credibility-item(data-mh="course")
      figure.credibility-media
        img(src= src, width="146", height="146", alt= title)
      p.credibility-summary !{summary}

mixin testimonial-item(link, title, src_mb, src_pc, content, address)
  .testimonial-item
    .testimonial-mb(style="background-image: url(" + src_mb + ")")
    .testimonial-pc(style="background-image: url(" + src_pc + ")")
    .testimonial-player
      i.fa.fa-play-circle
    .testimonial-cover
      .testimonial-table
        .testimonial-cell
          .testimonial-block
             p.testimonial-summary Những bài tập với thẻ cam giúp con mình hình dung ra khuôn mặt Pizza lúc chào đời đến 90%, cảm ơn thai giáo đã gắn kết bền chặt sợi dây tình cảm giữa hai mẹ con mình.
             p.testimonial-address Ngô Nam Phương - mẹ bé Pizza - Quận Hoàng Mai - HN

mixin video-item(link, title)
  .video-item
    a.owl-video(href=link)
    .video-title
      .video-table
        .video-cell= title